module basics.varaibles.mod_variables;

int i,j,k ;
char c, ch ;
float f, salary ;
double d ;

/*
When a varable is declared in D, it is always set to 
to its 'default initializer' which can be manually
accessed as T.init where T is the type (ex. int.init)

The defaut initializer for inter types is 0
the default initializer for booleans is false
the default for floating-point numbers is NaN
*/




